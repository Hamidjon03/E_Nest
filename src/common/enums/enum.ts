export enum RoleEnum {
  WORKER = 'worker',
  MANAGER = 'manager',
  ADMIN = 'admin',
  SUPERADMIN = 'superAdmin',
}

export enum StatusEnum {
  PROCESS = 'process',
  DONE = 'done',
  TOOK = 'took',
}
